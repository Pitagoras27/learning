// incorporations
// includes [strings, arrays] en lugar de indexOf

const pairNumbers = [2, 4, 6, 8];
console.log(pairNumbers.includes(7))

// operador de asignación de exponenciación
let x = 5
let y = 3

const result = x ** y
console.log(result)